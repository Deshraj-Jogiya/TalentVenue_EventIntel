{
  "name": "validate-feature-mappings",
  "version": "1.0.0",
  "private": true,
  "description": "Validates the shape of the ML pipeline's feature_mappings.json artifact",
  "scripts": {
    "build": "tsc",
    "validate": "node dist/validate.js",
    "test": "node --test dist/validate.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
